<% @page_title = "New Book Creation" %>

<section class="brownBox" id="longRollers">
    <div class="container">
         <h2 class="green thin pull-left noTop">&larr;<%= link_to "Back to Book list", books_path, :class => 'greenLink' %></h2>
           <div class="borderThick clearer"></div>
            <div class="addBookHold">

			<%= form_for @book, :html => {:multipart => true} do |f| %>

				<%= render(:partial => "form", :locals => {:f => f}) %>

				<div class="form-buttons">
				<%= submit_tag("Add This Book", :class => 'roundButton greenButton') %>
				</div>
			<% end %>
			</div>
	</div>      
</section>
