<% @page_title = "Comment by #{@comment.user.name} "%>
<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>
<div class="comment show">
	<h2>Comment by <%= @comment.user.name %></h2>
	<table summary="Comment detail view">
		<tr>
			<th>Comment</th>
			<td><%= @comment.comment %></td>
		</tr>
		<tr>
			<th>Book</th>
			<td><%= @comment.book.name %></td>
		</tr>
	</table>
</div>

